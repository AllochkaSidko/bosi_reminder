<Window x:Class="BoSi_Reminder.CabinetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BoSi_Reminder"
        mc:Ignorable="d"
        Title="CabinetWindow" Height="800" Width="1000">


    <Window.Resources>

        <Style TargetType="Button"
               x:Key="LogOutStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="DodgerBlue"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="DodgerBlue"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="15"/>
        </Style>

        <Style TargetType="Button"
               x:Key="CreateStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Orange"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="BorderBrush" Value="Orange"/>
            <Setter Property="FontWeight" Value="Heavy"/>
            <Setter Property="FontSize" Value="16pt"/>
        </Style>

        <Style TargetType="Button"
               x:Key="DeleteStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Crimson"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="BorderBrush" Value="Crimson"/>
            <Setter Property="FontWeight" Value="Heavy"/>
            <Setter Property="FontSize" Value="16pt"/>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="BorderBrush" Value="BlueViolet"/>
        </Style>

        <Style TargetType="Calendar">
            <Setter Property="BorderBrush" Value="DodgerBlue"/>
        </Style>
        <Style TargetType="CalendarButton">
            <Setter Property="Foreground" Value="OrangeRed"/>
        </Style>

    </Window.Resources>

    
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="70"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="600"></ColumnDefinition>
           
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0">
            <TextBlock Name="UsernameBlock" Text="Oleksandra Bosa" Foreground="BlueViolet" FontWeight="Medium" FontSize="22pt" Margin="20"></TextBlock>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="0">
            <DatePicker Name="DatePicker" SelectedDateFormat="Short" IsDropDownOpen="True" HorizontalAlignment="Left" Width="497" />
            <Popup Name="PopUp" StaysOpen="True"></Popup>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1">
            <TextBlock Name="DateBlock" Text="Wd, 09/27/2017" FontSize="24" Margin="10,10,0,0" Foreground="Orange"></TextBlock>
            <ListBox Height="800" Margin="10,20,15,0">

                <Grid Name="MessageBlock">
                    <TextBox Name="ReminderText" Height="60" TextWrapping="Wrap" Text="Do some stuff" Width="400" HorizontalAlignment="Right" Margin="100,20,0,0"/>
                    <TextBlock  Name="TimeBlock" Text="10.00 a.m." TextWrapping="Wrap" Margin="10,18,0,0" FontSize="17" Foreground="Orange"/>
                    <CheckBox Content="" HorizontalAlignment="Left" Margin="40,55,0,0"/>
                    <Button Name="DeleteReminder" Style="{StaticResource DeleteStyle}" Content="-" Width="40" Height="40" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,30,-55,0" />
                </Grid>
                

            </ListBox>
            
        </StackPanel>
  
        <Button Grid.Row="0" Grid.Column="1" Name="LogOutButton" Style="{StaticResource LogOutStyle}" Content="Log out" VerticalAlignment="Center" Height="30" Width="150"></Button>
        <Button Grid.Row="1" Grid.Column="1" Name="CreateReminder" Style="{StaticResource CreateStyle}" Content="+" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,8,25,0" Height="40" Width="40"></Button>
        
    </Grid>
</Window>
